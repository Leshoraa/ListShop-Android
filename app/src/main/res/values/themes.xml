<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ListShop" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="checkBoxStyle" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorTextPrimary</item>
        <item name="colorControlNormal">@color/colorTextPrimary</item>
        <item name="colorControlActivated">@color/colorTextPrimary</item>
    </style>
    <style name="Theme.ListShop" parent="Base.Theme.ListShop" />
</resources>